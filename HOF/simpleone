def sum(x: Int, y: Int): Int = {x+y}

def playwithtwonum(a: Int, b: Int, operation: (Int, Int)=> Int): Int = {
 operation(a,b)
}

val result = playwithtwonum(4,5,sum)

println(result)


########---Anonymous one----######
val result2 = operateOnTwoNumbers(4, 6, (x, y) => x + y)
println(result2) 
